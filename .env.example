# Database credentials (change in production!)
DB_USER=postgres
DB_PASSWORD=your-secure-database-password

# Redis password (change in production!)
REDIS_PASSWORD=your-secure-redis-password

# RabbitMQ credentials (change in production!)
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=your-secure-rabbitmq-password

# JWT Secret (MUST change in production - use at least 256 bits)
JWT_SECRET=your-256-bit-secret-key-here-change-in-production-minimum-32-chars
